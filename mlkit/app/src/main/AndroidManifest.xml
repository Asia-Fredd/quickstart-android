<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.google.firebase.samples.apps.mlkit">

    <uses-permission android:name="android.permission.CAMERA"/>
    <!--<uses-permission android:name="android.permission.INTERNET"/>-->
    <!--<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>-->

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialComponents.Bridge.TK3C"
        android:allowBackup="false"
        android:fullBackupContent="false"
        tools:ignore="AllowBackup">

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="ocr">
            <!--
            android:value="barcode,face,ocr,ica"
            -->
        </meta-data>

        <!--<activity android:name=".java.ChooserActivity" android:exported="false"/>-->

        <!--<activity android:name=".kotlin.ChooserActivity" android:exported="false"/>-->

        <!--<activity android:name=".EntryChoiceActivity" android:exported="false"/>-->

        <activity
            android:name=".java.LivePreviewActivity"
            android:screenOrientation="portrait"
            android:exported="true"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".kotlin.LivePreviewActivity" android:exported="false"/>

        <!--<activity android:name=".java.StillImageActivity" android:exported="false"/>-->

        <!--<activity android:name=".kotlin.StillImageActivity" android:exported="false"/>-->

        <activity android:name=".common.preference.SettingsActivity" android:exported="false"/>

    </application>

</manifest>
